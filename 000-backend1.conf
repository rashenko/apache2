#/etc/apache2/sites-available/000-default.conf

<VirtualHost *:80>

        ServerAdmin webmaster@localhost
        DocumentRoot /var/www/backend1

        ErrorLog ${APACHE_LOG_DIR}/error.backend1.log
        CustomLog ${APACHE_LOG_DIR}/access.backned1.log combined
</VirtualHost>
